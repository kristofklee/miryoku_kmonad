# Copyright 2021 Manna Harbour
# https://github.com/manna-harbour/miryoku

name: 'Build HHKB macOS'
on:
  - workflow_dispatch
jobs:
  build:
    uses: ./.github/workflows/main.yml
    with:
      mode: '["build"]'
      alphas: '[
        "qwerty"
      ]'
      nav: '[
        "vi"
      ]'
      clipboard: '[
        "mac"
      ]'
      layers: '[
        "default"
      ]'
      mapping: '[
        "default"
      ]'
      kmonad_os: '[
        "mac"
      ]'
